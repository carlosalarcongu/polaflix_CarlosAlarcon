========== src/main/resources/static/app.module.js ==========
angular.module('polaflixApp', ['ngRoute'])
    .config(['$locationProvider', '$routeProvider',
        function config($locationProvider, $routeProvider) {
            $locationProvider.hashPrefix('!');

            $routeProvider
                .when('/login', {
                    template: '<login-polaflix></login-polaflix>'
                })
                .when('/catalogo', {
                    template: '<catalogo-series></catalogo-series>'
                })
                .when('/series/:serieId', {
                    template: '<detalle-serie></detalle-serie>'
                })
                .when('/perfil', {
                    template: '<perfil-usuario></perfil-usuario>'
                })
                .when('/elenco', {
                    template: '<elenco-series></elenco-series>' 
                })
                .when('/comunidad', {
                    template: '<comunidad-polaflix></comunidad-polaflix>' 
                })
                .otherwise('/login');
        }
    ]);
    
angular.module('polaflixApp').controller('MainController', ['$scope', '$location', function($scope, $location) {
    $scope.mostrarMenu = function() {
        return $location.path() !== '/login';
    };
}]);


========== src/main/resources/static/catalogo/catalogo.component.js ==========
angular.module('polaflixApp').component('catalogoSeries', {
    templateUrl: 'catalogo/catalogo.template.html',
    controller: ['PolaflixService', '$location', '$q', function(PolaflixService, $location, $q) {
        var ctrl = this;
        ctrl.series = [];
        ctrl.username = sessionStorage.getItem('usuarioLogueado');

        ctrl.$onInit = function() {
            if (!ctrl.username) { $location.path('/login'); return; }
            
            $q.all([
                PolaflixService.getSeries(),
                PolaflixService.getUsuario(ctrl.username)
            ]).then(function(respuestas) {
                var catalogoData = respuestas[0];
                var usuarioData = respuestas[1];
                
                ctrl.series = catalogoData.map(function(serie) {
                    serie.estadoPersonal = usuarioData.estadoSeries[serie.titulo] || null;
                    return serie;
                });
            });
        };

        ctrl.addPendiente = function(idSerie) {
            PolaflixService.agregarAPendientes(ctrl.username, idSerie).then(function() {
                var serieAfectada = ctrl.series.find(s => s.id === idSerie);
                if (serieAfectada) serieAfectada.estadoPersonal = 'PENDIENTE';
            });
        };

        ctrl.getPosterGeneral = function(idSerie) {
            if (idSerie === 'S01') return 'images/PeakyBlinders.png';
            if (idSerie === 'S02') return 'images/PrisonBreakS01.png'; 
            if (idSerie === 'S03') return 'images/LQSAS01.png'; 
            return 'images/polaflix-logo.png'; 
        };

    }]
});


========== src/main/resources/static/catalogo/catalogo.template.html ==========
<section>
    <h2 style="font-size: 2em; margin-bottom: 40px; color: white;">CatÃ¡logo Completo</h2>
    
    <div class="shelf-container">
        <div class="shelf">
            <div class="card-serie" ng-repeat="s in $ctrl.series">
                
                <span class="badge badge-pendiente" ng-if="s.estadoPersonal === 'PENDIENTE'">ðŸ•’ PENDIENTE</span>
                <span class="badge badge-empezada" ng-if="s.estadoPersonal === 'EMPEZADA'">â–¶ï¸ EMPEZADA</span>
                <span class="badge badge-terminada" ng-if="s.estadoPersonal === 'TERMINADA'">âœ… TERMINADA</span>

                <img ng-src="{{$ctrl.getPosterGeneral(s.id)}}" class="serie-poster" alt="{{s.titulo}}">
                
                <div class="card-info">
                    <h4>{{s.titulo}}</h4>
                    <div style="display:flex; gap: 10px; justify-content: center;">
                        <a href="#!/series/{{s.id}}" class="btn-primary">Ver</a>
                        <button ng-if="!s.estadoPersonal" ng-click="$ctrl.addPendiente(s.id)" class="btn-secondary">âž• Lista</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>


========== src/main/resources/static/comunidad/comunidad.component.js ==========
angular.module('polaflixApp').component('comunidadPolaflix', {
    templateUrl: 'comunidad/comunidad.template.html',
    controller: ['PolaflixService', function(PolaflixService) {
        var ctrl = this;
        ctrl.searchUser = '';
        ctrl.userResult = null;
        ctrl.error = null;

        ctrl.buscar = function() {
            ctrl.error = null;
            ctrl.userResult = null;
            if (ctrl.searchUser) {
                PolaflixService.getUsuario(ctrl.searchUser.toLowerCase()).then(function(data) {
                    ctrl.userResult = data;
                }).catch(function() {
                    ctrl.error = "No se ha encontrado a ningÃºn usuario con ese nombre (Prueba con mrajoy, pepereina...).";
                });
            }
        };
    }]
});


========== src/main/resources/static/comunidad/comunidad.template.html ==========
<section class="fade-in">
    <h2 style="font-size: 2em; color: white;">ðŸŒ Actividad de la Comunidad</h2>
    <p style="color: #b3b3b3;">Busca a otros usuarios de la plataforma para descubrir quÃ© series estÃ¡n siguiendo.</p>
    
    <div style="display: flex; gap: 10px; margin-bottom: 30px;">
        <input type="text" ng-model="$ctrl.searchUser" placeholder="Ej: mrajoy, pepereina..." class="login-input" style="width: 300px; margin: 0;">
        <button ng-click="$ctrl.buscar()" class="btn-primary" style="font-size: 1.1em;">ðŸ” Buscar Perfil</button>
    </div>

    <div ng-if="$ctrl.error" class="error">{{$ctrl.error}}</div>

    <div ng-if="$ctrl.userResult" class="temporada-card" style="display: block;">
        <h3 style="color: var(--pola-red); font-size: 1.5em; margin-top: 0;">Historial de {{$ctrl.userResult.username}}</h3>
        
        <div ng-if="$ctrl.userResult.estadoSeries && ($ctrl.userResult.estadoSeries | json) != '{}'">
            <ul style="font-size: 1.1em; line-height: 2; list-style: none; padding: 0;">
                <li ng-repeat="(titulo, estado) in $ctrl.userResult.estadoSeries" style="background: #222; padding: 10px; margin-bottom: 5px; border-radius: 4px; display: flex; justify-content: space-between;">
                    <strong>{{titulo}}</strong>
                    <span class="badge" ng-class="{
                        'badge-terminada': estado === 'TERMINADA', 
                        'badge-pendiente': estado === 'PENDIENTE', 
                        'badge-empezada': estado === 'EMPEZADA'
                    }" style="position: static; box-shadow: none;">{{estado}}</span>
                </li>
            </ul>
        </div>
        <p ng-if="!$ctrl.userResult.estadoSeries || ($ctrl.userResult.estadoSeries | json) == '{}'" style="color: #888;">
            Este usuario es muy reservado y aÃºn no tiene series en su lista.
        </p>
    </div>
</section>


========== src/main/resources/static/elenco/elenco.component.js ==========
angular.module('polaflixApp').component('elencoSeries', {
    templateUrl: 'elenco/elenco.template.html',
    controller: ['PolaflixService', '$q', function(PolaflixService, $q) {
        var ctrl = this;
        ctrl.actoresMap = {};
        ctrl.cargando = true;

        ctrl.$onInit = function() {
            PolaflixService.getSeries().then(function(seriesResumidas) {
                
                var promesasDetalle = seriesResumidas.map(s => PolaflixService.getSerie(s.id));
                
                $q.all(promesasDetalle).then(function(seriesDetalladas) {
                    seriesDetalladas.forEach(function(serie) {
                        if (serie && serie.actores) {
                            serie.actores.forEach(function(actor) {
                                if (!ctrl.actoresMap[actor.nombre]) {
                                    ctrl.actoresMap[actor.nombre] = [];
                                }
                                ctrl.actoresMap[actor.nombre].push(serie.titulo);
                            });
                        }
                    });
                    ctrl.cargando = false;
                });
            });
        };
    }]
});


========== src/main/resources/static/elenco/elenco.template.html ==========
<section class="fade-in">
    <h2 style="font-size: 2em; color: white;">ðŸŽ­ Directorio de Actores</h2>
    <p style="color: #b3b3b3;">Descubre en quÃ© series del catÃ¡logo de Polaflix participan tus actores favoritos.</p>

    <div ng-if="$ctrl.cargando">
        <h3 style="color: var(--pola-red);"><blink>Revisando los contratos de los actores...</blink></h3>
    </div>

    <div ng-if="!$ctrl.cargando" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 20px; margin-top: 30px;">
        <div class="temporada-card" style="display: block; padding: 15px;" ng-repeat="(actor, listaSeries) in $ctrl.actoresMap">
            <h3 style="margin-top: 0; color: white; border-bottom: 1px solid #444; padding-bottom: 10px;">{{actor}}</h3>
            <ul style="color: #bbb; padding-left: 20px;">
                <li ng-repeat="tituloSerie in listaSeries" style="margin-bottom: 5px;">{{tituloSerie}}</li>
            </ul>
        </div>
    </div>
</section>


========== src/main/resources/static/index.html ==========
<!DOCTYPE html>
<html lang="es" ng-app="polaflixApp">
<head>
    <meta charset="UTF-8">
    <title>Polaflix - Tu Cine en Casa</title>
    <link rel="stylesheet" href="polaflix.css">
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.6.4/angular.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.6.4/angular-route.js"></script>
    
    <script src="app.module.js"></script>
    <script src="polaflix.service.js"></script>
    <script src="login/login.component.js"></script>
    <script src="catalogo/catalogo.component.js"></script>
    <script src="serie/serie.component.js"></script>
    <script src="perfil/perfil.component.js"></script>
    
    <script src="elenco/elenco.component.js"></script>
    <script src="comunidad/comunidad.component.js"></script>
</head>
<body ng-controller="MainController">

    <aside class="sidebar" ng-show="mostrarMenu()">
        <a href="#!/catalogo">
            <img src="images/polaflix-logo.png" class="sidebar-logo" alt="Polaflix">
        </a>
        <nav>
            <a href="#!/catalogo">CatÃ¡logo</a>
            <a href="#!/perfil">Mi Perfil</a>
            <a href="#!/elenco">Elenco y Actores</a>
            <a href="#!/comunidad">Comunidad</a>
            <a href="#!/login" style="margin-top: 50px; color: #666;">ðŸšª Cambiar Usuario</a>
        </nav>
    </aside>

    <main class="main-content">
        <div ng-view></div>
    </main>

</body>
</html>


========== src/main/resources/static/login/login.component.js ==========
angular.module('polaflixApp').component('loginPolaflix', {
    templateUrl: 'login/login.template.html',
    controller: ['$location', function LoginController($location) {
        var ctrl = this;
        ctrl.username = '';

        ctrl.entrar = function() {
            if (ctrl.username) {
                sessionStorage.setItem('usuarioLogueado', ctrl.username);
                $location.path('/series/S01'); 
            }
        };
    }]
});


========== src/main/resources/static/login/login.template.html ==========
<div class="login-wrapper">
    <div class="login-box">
        <img src="images/polaflix-logo.png" class="login-logo" alt="Polaflix Logo">
        
        <input type="text" class="login-input" ng-model="$ctrl.username" placeholder="Introduce tu usuario (ej: cr7bicho)">
        
        <button class="login-btn" ng-click="$ctrl.entrar()">ENTRAR AL SISTEMA</button>
    </div>
</div>


========== src/main/resources/static/perfil/perfil.component.js ==========
angular.module('polaflixApp').component('perfilUsuario', {
    templateUrl: 'perfil/perfil.template.html',
    controller: ['PolaflixService', '$location', function(PolaflixService, $location) {
        var ctrl = this;
        ctrl.usuario = null;
        ctrl.username = sessionStorage.getItem('usuarioLogueado');

        ctrl.$onInit = function() {
            if (!ctrl.username) { $location.path('/login'); return; }
            PolaflixService.getUsuario(ctrl.username).then(function(data) {
                ctrl.usuario = data;
            });
        };
    }]
});


========== src/main/resources/static/perfil/perfil.template.html ==========
<div ng-if="$ctrl.usuario" class="fade-in">
    <h2>Perfil de {{$ctrl.usuario.username}}</h2>
    <div style="background: #222; padding: 20px; border-radius: 8px; margin-bottom: 30px;">
        <p><strong>Plan de SuscripciÃ³n:</strong> {{$ctrl.usuario.planSuscripcion.tarifaPlana ? 'Tarifa Plana VIP' : 'Pago por VisiÃ³n'}}</p>
        <p><strong>IBAN:</strong> {{$ctrl.usuario.iban.numeroCuenta}}</p>
    </div>

    <h3>Resumen de tus Series</h3>
    <ul style="font-size: 1.1em; line-height: 1.8;">
        <li ng-repeat="(titulo, estado) in $ctrl.usuario.estadoSeries">
            <strong>{{titulo}}</strong>: 
            <span ng-class="{
                'success': estado === 'TERMINADA', 
                'error': estado === 'PENDIENTE', 
                'info': estado === 'EMPEZADA'
            }">{{estado}}</span>
        </li>
    </ul>
    <p ng-if="!$ctrl.usuario.estadoSeries">AÃºn no tienes actividad en tu cuenta.</p>
</div>


========== src/main/resources/static/polaflix.css ==========
:root { --pola-red: #E50914; --dark-bg: #141414; --shelf-wood: #3e2723; }
body { margin: 0; background-color: var(--dark-bg); color: white; display: flex; height: 100vh; overflow: hidden; font-family: 'Segoe UI', Tahoma, sans-serif; }

.sidebar { width: 260px; background: #000; border-right: 1px solid #333; display: flex; flex-direction: column; padding: 30px 20px; box-shadow: 2px 0 10px rgba(0,0,0,0.5); z-index: 100; }
.sidebar-logo { width: 100%; margin-bottom: 50px; cursor: pointer; transition: transform 0.2s; }
.sidebar-logo:hover { transform: scale(1.05); }
.sidebar nav { display: flex; flex-direction: column; gap: 20px; }
.sidebar nav a { color: #b3b3b3; text-decoration: none; font-size: 1.2em; font-weight: bold; transition: color 0.3s; display: flex; align-items: center; gap: 10px; }
.sidebar nav a:hover { color: var(--pola-red); }
.main-content { flex-grow: 1; padding: 40px; overflow-y: auto; background: linear-gradient(to bottom, #1f1f1f, var(--dark-bg)); }

.login-wrapper { display: flex; justify-content: center; align-items: center; height: 100%; flex-direction: column; }
.login-box { background: rgba(0,0,0,0.85); padding: 50px 60px; border-radius: 8px; text-align: center; border: 1px solid #333; width: 400px; box-shadow: 0 10px 30px rgba(0,0,0,0.8); }
.login-logo { width: 100%; max-width: 300px; margin-bottom: 40px; }
.login-input { padding: 15px; width: 100%; box-sizing: border-box; margin-bottom: 25px; border-radius: 5px; border: none; font-size: 1.1em; background: #333; color: white; }
.login-btn { background: var(--pola-red); color: white; padding: 15px; width: 100%; border: none; border-radius: 5px; font-size: 1.2em; font-weight: bold; cursor: pointer; transition: background 0.2s; }
.login-btn:hover { background: #b20710; }

.shelf-container { margin-top: 20px; margin-bottom: 60px; }
.shelf { display: flex; gap: 25px; padding-bottom: 10px; border-bottom: 15px solid var(--shelf-wood); box-shadow: inset 0 -5px 10px rgba(0,0,0,0.6); }
.card-serie { width: 220px; position: relative; transition: transform 0.3s; background: #111; border-radius: 4px; overflow: hidden; }
.card-serie:hover { transform: translateY(-15px); box-shadow: 0 15px 20px rgba(0,0,0,0.5); }
.serie-poster { width: 100%; height: 310px; object-fit: cover; }
.card-info { padding: 15px; text-align: center; }
.card-info h4 { margin: 0 0 10px 0; font-size: 1.1em; color: #fff; }

.badge { position: absolute; top: 10px; right: 10px; padding: 5px 10px; font-size: 0.8em; font-weight: bold; border-radius: 3px; z-index: 10; box-shadow: 0 2px 5px rgba(0,0,0,0.5); }
.badge-pendiente { background: #f39c12; color: #000; }
.badge-empezada { background: #3498db; color: #fff; }
.badge-terminada { background: #2ecc71; color: #fff; }

.btn-primary { background: var(--pola-red); color: white; padding: 8px 12px; text-decoration: none; border-radius: 3px; font-weight: bold; border: none; cursor: pointer; }
.btn-secondary { background: #555; color: white; padding: 8px 12px; border: none; border-radius: 3px; font-weight: bold; cursor: pointer; }
.btn-secondary:hover { background: #777; }

.header-detalle { display: flex; gap: 40px; margin-bottom: 40px; }
.poster-detalle { width: 300px; border-radius: 8px; box-shadow: 0 0 20px rgba(0,0,0,0.5); }
.temporada-card { display: flex; gap: 25px; background: #1a1a1a; padding: 25px; border-radius: 8px; margin-bottom: 25px; border-left: 5px solid var(--pola-red); }
.temporada-poster { width: 160px; height: 240px; border-radius: 6px; object-fit: cover; box-shadow: 0 4px 8px rgba(0,0,0,0.5); }
.capitulos-grid { display: flex; gap: 15px; flex-wrap: wrap; margin-top: 20px; }

.capitulo-btn { 
    width: 80px; 
    height: 80px; 
    border-radius: 12px; 
    background-color: #222; 
    color: #fff; 
    border: 3px solid #555; 
    cursor: pointer; 
    transition: all 0.2s ease-in-out; 
    
    display: flex; 
    flex-direction: column; 
    align-items: center; 
    justify-content: center; 
    
    box-shadow: 0 4px 6px rgba(0,0,0,0.3);
    padding: 5px; 
    box-sizing: border-box; 
}

.capitulo-btn:hover { 
    background-color: var(--pola-red); 
    border-color: #fff; 
    transform: scale(1.1); 
    box-shadow: 0 6px 12px rgba(229, 9, 20, 0.5); 
}

.num-gigante {
    font-size: 28px;
    font-weight: bold;
    line-height: 1;
    margin-bottom: 4px;
}

.texto-pequeno {
    font-size: 9px;
    font-weight: normal;
    color: #aaa;
    white-space: nowrap; 
    overflow: hidden; 
    text-overflow: ellipsis; 
    width: 100%; 
    text-align: center;
    transition: color 0.2s;
}

.capitulo-btn:hover .texto-pequeno {
    color: #fff;
}.error { color: #ff6b6b; font-weight: bold; }
.success { color: #46d369; font-weight: bold; }


========== src/main/resources/static/polaflix.service.js ==========
angular.module('polaflixApp').factory('PolaflixService', ['$http', '$q', function($http, $q) {
    return {
        
        getSeries: function() {
            return $http.get('/series')
                .then(res => res.data)
                .catch(err => $q.reject("No se pudo cargar el catÃ¡logo."));
        },

        
        getSerie: function(id) {
            return $http.get('/series/' + id)
                .then(res => res.data)
                .catch(err => $q.reject("Serie no encontrada."));
        },

        verCapitulo: function(user, capId) {
            return $http.put(`/usuarios/${user}/capitulos-vistos/${capId}`)
                .then(function(response) {
                    return "Â¡CapÃ­tulo marcado como visto con Ã©xito!"; 
                })
                .catch(function(err) {
                    return $q.reject("Error al registrar visualizaciÃ³n.");
                });
        },

        agregarAPendientes: function(user, serieId) {
            return $http.put(`/usuarios/${user}/series-pendientes/${serieId}`)
                .catch(err => $q.reject("No se pudo aÃ±adir a pendientes."));
        },

        
        getUsuario: function(username) {
            return $http.get('/usuarios/' + username)
                .then(function(response) { return response.data; })
                .catch(function(error) { return $q.reject("No se pudo cargar el perfil."); });
        }
    };
}]);


========== src/main/resources/static/serie/serie.component.js ==========
angular.module('polaflixApp').component('detalleSerie', {
    templateUrl: 'serie/serie.template.html',
    controller: ['$routeParams', 'PolaflixService', function SerieController($routeParams, PolaflixService) {
        var ctrl = this;
        
        ctrl.serie = null;
        ctrl.cargando = true;
        ctrl.mensajeError = null;
        ctrl.mensajeExito = null;
        
        ctrl.username = sessionStorage.getItem('usuarioLogueado') || 'cr7bicho'; 

        var serieId = $routeParams.serieId;

        ctrl.$onInit = function() {
            PolaflixService.getSerie(serieId).then(function(data) {
                ctrl.serie = data;
                ctrl.cargando = false;
            }).catch(function(error) {
                ctrl.mensajeError = error;
                ctrl.cargando = false;
            });
        };

        ctrl.marcarVisto = function(idCapitulo) {
            ctrl.mensajeError = null;
            ctrl.mensajeExito = null;
            
            PolaflixService.verCapitulo(ctrl.username, idCapitulo).then(function(mensaje) {
                ctrl.mensajeExito = mensaje;
            }).catch(function(error) {
                ctrl.mensajeError = error;
            });
        };


        ctrl.getPosterGeneral = function(idSerie) {
            if (idSerie === 'S01') return 'images/PeakyBlinders.png';
            if (idSerie === 'S02') return 'images/PrisonBreakS01.png';
            if (idSerie === 'S03') return 'images/LQSAS01.png';
            return 'images/polaflix-logo.png';
        };

        ctrl.getPosterTemporada = function(idSerie, numTemporada) {
            var safeNum = numTemporada > 4 ? 1 : numTemporada;
            var sufijo = 'S0' + safeNum + '.png';

            if (idSerie === 'S01') return 'images/PeakyBlinders' + sufijo;
            if (idSerie === 'S02') return 'images/PrisonBreak' + sufijo;
            if (idSerie === 'S03') return 'images/LQSA' + sufijo;
            
            return 'images/polaflix-logo.png';
        };
    }]
});


========== src/main/resources/static/serie/serie.template.html ==========
<div ng-if="$ctrl.serie" class="fade-in">
    
    <div class="header-detalle">
        <img ng-src="{{$ctrl.getPosterGeneral($ctrl.serie.id)}}" class="poster-detalle">
        <div class="info-texto">
            <h1 style="color: var(--pola-red); margin-top: 0; font-size: 3em;">{{$ctrl.serie.titulo}}</h1>
            <p class="sinopsis" style="font-size: 1.2em; line-height: 1.6;">{{$ctrl.serie.sinopsis}}</p>
            <p><strong>CategorÃ­a:</strong> <span class="badge badge-empezada">{{$ctrl.serie.categoria}}</span></p>
            <p><strong>Precio por CapÃ­tulo:</strong> {{$ctrl.serie.costeVisionado}} â‚¬</p>
        </div>
    </div>

    <p ng-if="$ctrl.mensajeError" class="error">{{$ctrl.mensajeError}}</p>
    <p ng-if="$ctrl.mensajeExito" class="success">{{$ctrl.mensajeExito}}</p>

    <div class="listado-episodios" style="margin-top: 40px;">
        <h2 style="border-bottom: 1px solid #444; padding-bottom: 10px;">Episodios</h2>

        <div class="temporada-card" ng-repeat="temp in $ctrl.serie.temporadas | orderBy:'numero'">
            
            <img ng-src="{{$ctrl.getPosterTemporada($ctrl.serie.id, temp.numero)}}" class="temporada-poster">
            
            <div class="temporada-info">
                <h3 style="margin-top: 0;">Temporada {{temp.numero}}</h3>
                <p style="color: #999;">Selecciona un nÃºmero para marcar el capÃ­tulo como visto:</p>
                
                <div class="capitulos-grid">
                    <button class="capitulo-btn" 
                            title="{{cap.titulo}}" 
                            ng-repeat="cap in temp.capitulos | orderBy:'numero'" 
                            ng-click="$ctrl.marcarVisto(cap.id)">
                        <span class="num-gigante">{{cap.numero}}</span>
                        <span class="texto-pequeno">{{cap.titulo}}</span>
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>


